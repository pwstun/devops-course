apiVersion: v1
kind: Service
metadata:
  name: {{ include "myapp.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "myapp.name" . }}
  ports:
    - port: 80
      targetPort: 8080